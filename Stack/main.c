#include "stack.h"

int main (void){

	int top= -1;
		insert (&stack,5,&top);
		insert (&stack,6,&top);
		insert (&stack,7,&top);
		  show_delete(&stack, &top);
		  show_delete(&stack, &top);
		  show_delete(&stack, &top);
		return 0;

}

